version: 2

project_name: kubectl-ondemand

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: kubectl-ondemand
    binary: kubectl-ondemand
    main: ./cmd/kubectl-ondemand
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: ssoriche
    name: kubectl-ondemand
  draft: false
  prerelease: auto
  mode: append
  footer: |
    ## Installation

    ### Manual installation
    Download the appropriate binary for your platform, extract it, and place it in your PATH.

    ```bash
    # Example for macOS (Apple Silicon)
    tar -xzf kubectl-ondemand_darwin_arm64.tar.gz
    chmod +x kubectl-ondemand
    sudo mv kubectl-ondemand /usr/local/bin/
    ```
